{% extends 'SyliusWebBundle:Frontend:layout.html.twig' %}

{% block slideshow_full %}
    <div class="row">
        {{ sonata_block_render({'name': '/cms/blocks/slideshow-full'}) }}
    </div>
{% endblock %}

{% block slideshow_sidebar %}
    <div class="row">
        {{ sonata_block_render({'name': '/cms/blocks/slideshow-sidebar'}) }}
    </div>
{% endblock %}


{% block content %}

<div class="jumbotron">
    <h1><span class="name"><span style="color:#8DBF43 !important;">{{ 'sylius.homepage.splash.headline'|trans|slice(0, 7) }}</span><span style="color:#404041 !important;margin-left: 0px;">.</span><span style="color:#1892B6 !important;margin-left: 0px;">{{ 'sylius.homepage.splash.headline'|trans|slice(8, 10) }}</span></h1>
    <p class="lead">{{ 'sylius.homepage.splash.subheadline'|trans }}</p>
</div>

{{ sonata_block_render({'name': '/cms/blocks/slideshow-home'}) }}

{{ render(url('sylius_partial_product_latest', {'limit': 9, 'template': 'SyliusWebBundle:Frontend/Product:latest.html.twig'})) }}

{% endblock %}
